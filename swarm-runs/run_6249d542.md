# Add discount codes to checkout

Support percentage and fixed-amount discount codes at checkout. Codes have expiry dates and usage limits. Apply before tax. New endpoint POST /checkout/apply-discount.

_Swarm run `run_6249d542` · 15634 tokens · $0.0304_

## Decisions
- **discount-type-enum** — Discount codes store type as enum: PERCENTAGE | FIXED_AMOUNT, with percentage capped at 0-100 and fixed_amount as positive decimal(10,2). (95%)
- **discount-application-order** — Apply discount codes to subtotal *before* tax calculation; store applied_discount_amount on checkout record for audit. (92%)
- **usage-limit-enforcement** — Track usage_count (integer) and max_uses (nullable integer) per code; enforce atomically with SELECT...FOR UPDATE on discount row before incrementing. (88%)
- **expiry-validation-timing** — Validate expires_at >= NOW() at POST /checkout/apply-discount time; reject with 400 Bad Request and error code DISCOUNT_EXPIRED. (90%)
- **endpoint-idempotency** — POST /checkout/apply-discount accepts idempotency_key header; reapplying same code to same checkout is idempotent (returns 200, same discount). (85%)

## Review: PASS

## Contradictions resolved
- **idempotency-requirement**: Architect's decision explicitly specifies idempotency_key header and idempotent behavior. Skeptic raises valid risk but does not contradict the decision—rather, skeptic flags that the decision was made. Architect has higher access (decision-maker) and explicitly addressed this concern. No contradiction; skeptic's concern is acknowledged by architect's design choice. (policy 2)
- **discount-application-timing-tax**: Architect asserts discount applied before tax. Skeptic asserts this may violate regulations in some jurisdictions. These are incompatible claims about the same subject (tax calculation order). Architect has higher access (decision-maker with reasoning). Skeptic raises a valid regulatory risk but does not provide a correction from a human authority or higher-access source. Architect's decision stands; skeptic's concern should be logged as a risk for human review but does not override the architect's decision. (policy 2)
- **usage-limit-enforcement-scope**: Architect specifies usage limits are per-code (global), not per-user. Skeptic claims ambiguity about whether limits are per-user. Architect's decision explicitly states 'per code' with no per-user dimension. This is not a contradiction of the architect's decision but a skeptic concern about missing per-user limits. Architect's stated design is unambiguous; skeptic's concern is a feature gap, not a contradiction. (policy 2)
- **race-condition-on-usage-limits**: Architect specifies atomic enforcement via SELECT...FOR UPDATE, which prevents race conditions. Skeptic asserts race conditions are possible. These are directly incompatible. Architect has higher access (decision-maker with technical reasoning). Architect's atomic locking strategy explicitly addresses the skeptic's concern. No human correction provided. Architect's decision is the resolution. (policy 2)